import styled from "styled-components";

const CollectionsArea = styled.section`
  border-bottom: 2px solid var(--colorPrimary);

  a {
    color: var(--colorBlack);
  }

  img {
    height: 60%;
  }
`;

export default CollectionsArea;
