import Logo from "../Logo";
import HeaderContainer from "./HeaderContainer";

const Header = () => {
  return (
    <HeaderContainer>
      <Logo />
    </HeaderContainer>
  );
};

export default Header;
